<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Scheduled Action: Update Statement Entries (Add Missing Months) -->
    <record id="ir_cron_update_statement_entries" model="ir.cron">
        <field name="name">Update Statement Entries (Add Missing Months)</field>
        <field name="model_id" ref="model_property_agreement"/>
        <field name="state">code</field>
        <field name="code">model.cron_update_statement_entries()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="True"/>
    </record>
</odoo>
